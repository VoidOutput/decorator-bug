{
  "name": "decorator-bug",
  "description": "A minimal reproducible example of a decorator bug",
  "scripts": {
    "problem": "babel src/problem --out-dir out/problem",
    "no-problem": "babel src/no-problem --out-dir out/no-problem"
  },
  "dependencies": {
    "@babel/cli": "^7.1.0",
    "@babel/core": "^7.1.0",
    "@babel/plugin-proposal-decorators": "^7.1.0"
  }
}
